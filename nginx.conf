server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    location /media/ {
        alias /media/;
        autoindex off;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}